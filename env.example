# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Required for API mode: Linear API key
# Get from Linear Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_...

# Optional: Linear webhook signing secret (verifies webhook authenticity)
# Get from Linear Settings → API → Webhooks → Your webhook → Signing secret
LINEAR_WEBHOOK_SECRET=...

# Optional: Directory for synced data (default: ./data)
DOCS_DIR=./data

# Optional: Sync interval in hours for scheduled data refresh (default: 1)
SYNC_INTERVAL_HOURS=1

# Optional: Comma-separated list of Linear project names to exclude from enhancement
# Issues created in these projects will be skipped (case-insensitive)
LINEAR_EXCLUDED_PROJECTS=Internal,Housekeeping

# Optional: Email domains for tagging internal vs external users (comma-separated)
INTERNAL_DOMAINS=trelent.com,trelent.net

# GitHub CLI token for code analysis (used by agents, not a sync connector)
# Create at: https://github.com/settings/tokens with 'repo' scope
# For local dev, you can use `gh auth login` instead
GH_TOKEN=ghp_...

# -----------------------------------------------------------------------------
# DATA CONNECTORS
# Each connector is auto-enabled when its env var is set.
# Remove or leave empty to disable a connector.
# -----------------------------------------------------------------------------

# SLACK CONNECTOR
# User token (xoxp-) or bot token (xoxb-)
# User tokens access all channels you're in without adding a bot
# Required scopes: channels:history, channels:read, groups:history, groups:read,
#   users:read, users:read.email, im:history, im:read, mpim:history, mpim:read
SLACK_TOKEN=xoxp-...

# GOOGLE DRIVE CONNECTOR
# Option 1 (local): Path to service account JSON file
GDRIVE_CREDS=./credentials/gdrive-service-account.json
# Option 2 (deployed): Base64-encoded JSON content
# Generate with: cat credentials/gdrive-service-account.json | base64
# GDRIVE_CREDS_BASE64=eyJ0eXBlIjoi...

# GMAIL CONNECTOR
# Requires explicit opt-in (uses same Google creds as GDrive)
# GMAIL_ENABLED=true
#
# For service accounts with domain-wide delegation, specify which user to impersonate
# GMAIL_USER_EMAIL=your-email@yourcompany.com
#
# Allow-list of senders (comma-separated emails or @domains)
# Emails from Slack users are automatically included!
# GMAIL_ALLOWED_SENDERS=partner@external.com,@trusted-vendor.com
#
# Note: INTERNAL_DOMAINS emails are always allowed
# Spam, trash, promotions, and social emails are excluded automatically
