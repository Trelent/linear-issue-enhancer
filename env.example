# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Required: Slack user token (xoxp-) or bot token (xoxb-)
# User tokens access all channels you're in without adding a bot
# See src/sync/config.py for required OAuth scopes
SLACK_TOKEN=xoxp-...

# Optional: Google Drive credentials
# Option 1 (local): Path to service account JSON file
GDRIVE_CREDS=./credentials/gdrive-service-account.json

# Option 2 (deployed): Base64-encoded JSON content
# Generate with: cat credentials/gdrive-service-account.json | base64
# GDRIVE_CREDS_BASE64=eyJ0eXBlIjoi...

# Optional: Email domains for tagging internal vs external users (comma-separated)
INTERNAL_DOMAINS=trelent.com,trelent.net

# Required for API mode: Linear API key
# Get from Linear Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_...

# Optional: Linear webhook signing secret (verifies webhook authenticity)
# Get from Linear Settings → API → Webhooks → Your webhook → Signing secret
LINEAR_WEBHOOK_SECRET=...

# Optional: Directory for synced data (default: ./data)
DOCS_DIR=./data

# Optional: Sync interval in hours for scheduled data refresh (default: 1)
SYNC_INTERVAL_HOURS=1

# Optional: GitHub token for gh CLI (needed in Docker/deployed environments)
# Create at: https://github.com/settings/tokens
GH_TOKEN=ghp_...

# Optional: Comma-separated list of Linear project names to exclude from enhancement
# Issues created in these projects will be skipped (case-insensitive)
LINEAR_EXCLUDED_PROJECTS=Internal,Housekeeping
